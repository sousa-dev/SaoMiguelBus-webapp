/**
 * PDL MiniBus's brand accent (orange) — the module's visual signature next to
 * the app-wide green the Autocarros/AzoresBus module uses. Single source of
 * truth for every MiniBus-specific icon, border and highlight, mirroring
 * mobile's `features/minibus/lib/moduleAccent.ts`.
 */
export const MINIBUS_ACCENT = '#f47216';
